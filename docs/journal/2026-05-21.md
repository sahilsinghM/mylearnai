# Journal — 2026-05-21

## What shipped

**Socratic Tutor — Proof Loop (Phase 1)**

Built the full tutor feature end-to-end: streaming Socratic chat, gap analysis on session close, and project assignment generation. Seven files added or modified.

### Files
- `src/types/tutor.ts` — `Message`, `Gap`, `ProjectAssignment`, `TutorSessionResult` types
- `src/lib/anthropic/prompts.ts` — `buildTutorSystemPrompt` and `buildCloseSessionPrompt`
- `src/app/api/tutor/chat/route.ts` — SSE streaming endpoint, bootstrap injection
- `src/app/api/tutor/close-session/route.ts` — gap extraction + project assignment via Claude
- `src/app/(app)/tutor/page.tsx` — server component, Supabase week context lookup
- `src/components/tutor/TutorChat.tsx` — full chat UI with streaming, retry, end-session flow
- `src/components/layout/Sidebar.tsx` — added Tutor nav item

### Architecture decisions
- **SSE format**: raw text chunks (not JSON-wrapped). Error sentinel is JSON `{"error":true}` to avoid collision with real Claude text.
- **Page structure**: server wrapper (`tutor/page.tsx`) + client child (`TutorChat.tsx`) — matches existing dashboard/plan pattern. Supabase auth and week context resolved server-side.
- **`cleanHistory` ref**: tracks only fully-completed turns for API calls. Display state (`messages`) stays separate. Prevents partial turns from being sent to the API on retry or end-session.
- **Bootstrap**: empty `useEffect` fires on mount → calls `streamAssistant([])` → API injects `"Begin the session."` → Claude opens with first question.

### Bugs fixed during review
1. **SSE newline escaping** — Claude text chunks can contain `\n`. Raw `data: ${text}\n\n` breaks SSE framing. Fixed: escape `\n` → `\\n` on emit, unescape on client.
2. **`endSession` concurrent race** — `endSession` didn't guard on `isSending`. If a stream was in-flight, close-session would call with an incomplete `cleanHistory`. Fixed: added `isSending` check.

### Bugs fixed during QA (post-ship)
3. **Duplicate assistant bubbles** — `cleanHistory.current` was mutated inside a `setMessages` functional updater. React 18 StrictMode double-invokes updaters to surface side effects, so each assistant turn was appended to `cleanHistory` twice, causing every message to render twice. Fixed: accumulate stream content in a local `accumulated` variable, commit to the ref once outside any setState callback.
4. **Premature End Session guard** — guard checked `assistantTurns.length < 1`, but the bootstrap puts one assistant message into `cleanHistory` before the user types anything. Clicking End Session immediately after page load would bypass the guard and call close-session with no user input. Fixed: guard now checks `userTurns.length < 1`.

Both fixes shipped as commit `04b3f52`.

## QA results

Tested all 5 acceptance scenarios from TODOS.md:

| Scenario | Result |
|---|---|
| Mentor asks first on mount | ✅ Pass |
| Streaming + multi-turn exchange | ✅ Pass |
| End Session returns project card | ✅ Pass |
| End Session before any exchange → error | ✅ Pass (required fix #4) |
| End Session disabled during stream | ✅ Pass (code-verified: `disabled={isEnding \|\| isSending}`) |

## Deferred (TODOS.md P2)
- `tutor_sessions` DB table — persist gaps and project assignment. Blocked on: validate the core loop with real users first.
- Wire project assignment to `/project` UI. Depends on DB persistence.

## Pipeline run
Went through the full `/autoplan` pipeline: CEO review → Design review → Eng review. All findings resolved before ship.

## Build
`bun run build` — clean, no TypeScript errors.
